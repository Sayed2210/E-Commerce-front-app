---
description: Start the Nuxt dev server
---

Start the Nuxt development server and verify it loads without errors.

Current server status:
!`ps aux | grep "nuxt dev" | grep -v grep || echo "No dev server running"`

If not running, start it with: `npm run dev`
